# 📦 导入导出功能测试指南

## ✅ 功能概述

新的导入导出功能现在支持**完整的数据备份**，包括：

- ✅ 工作区（Workspaces）
- ✅ 项目（Projects）
- ✅ 作品（Tracks）
- ✅ 活跃状态（当前选中的工作区和项目）

## 🔧 功能改进

### 之前的问题
- ❌ 只导出作品数据
- ❌ 不包含工作区和项目
- ❌ 导入后需要手动重建项目结构

### 现在的改进
- ✅ 导出完整的数据结构
- ✅ 包含所有工作区、项目和作品
- ✅ 保留活跃状态
- ✅ 智能合并（基于更新时间）
- ✅ 导入后自动刷新页面

## 📋 测试步骤

### 1. 准备测试数据

在应用中创建一些测试数据：

```
工作区1: 个人项目
  └─ 项目A: 专辑制作
      ├─ 作品1: 歌曲A
      ├─ 作品2: 歌曲B
      └─ 作品3: 歌曲C
  └─ 项目B: 单曲发行
      └─ 作品4: 单曲D

工作区2: 客户项目
  └─ 项目C: 商业配乐
      └─ 作品5: 配乐E
```

### 2. 测试导出功能

#### 步骤：
1. 点击顶部导航栏的 **💾** 图标
2. 浏览器会自动下载文件：`pattr-backup-YYYY-MM-DD.json`
3. 打开下载的 JSON 文件

#### 验证内容：
```json
{
  "version": "2.0",
  "exportedAt": "2025-12-11T...",
  "workspaces": [
    {
      "id": "...",
      "name": "个人项目",
      ...
    },
    {
      "id": "...",
      "name": "客户项目",
      ...
    }
  ],
  "projects": [
    {
      "id": "...",
      "name": "专辑制作",
      "workspaceId": "...",
      ...
    },
    ...
  ],
  "tracks": [
    {
      "id": "...",
      "name": "歌曲A",
      "projectId": "...",
      ...
    },
    ...
  ],
  "activeWorkspaceId": "...",
  "activeProjectId": "..."
}
```

#### 检查项：
- [ ] 文件包含 `version: "2.0"`
- [ ] 包含所有工作区
- [ ] 包含所有项目
- [ ] 包含所有作品
- [ ] 包含活跃状态
- [ ] 数据结构完整

### 3. 测试导入功能

#### 场景 A: 导入到空数据库

1. **清空本地数据**：
   - 打开浏览器 DevTools (F12)
   - 进入 Application → Local Storage
   - 删除所有 `pattr_*` 键
   - 刷新页面

2. **导入备份**：
   - 点击顶部导航栏的 **📥** 图标
   - 选择之前导出的 JSON 文件
   - 等待导入完成

3. **验证结果**：
   - [ ] 显示成功消息：`✅ 成功导入：X 个工作区、Y 个项目、Z 个作品`
   - [ ] 页面自动刷新
   - [ ] 所有工作区都恢复了
   - [ ] 所有项目都恢复了
   - [ ] 所有作品都恢复了
   - [ ] 活跃状态正确恢复

#### 场景 B: 导入到已有数据（合并）

1. **修改一些数据**：
   - 编辑一个作品的名称
   - 添加一个新作品
   - 删除一个旧作品

2. **导入旧备份**：
   - 点击 **📥** 图标
   - 选择之前的备份文件

3. **验证智能合并**：
   - [ ] 新添加的作品仍然存在
   - [ ] 修改过的数据保留最新版本（基于 `updatedAt`）
   - [ ] 没有重复数据
   - [ ] 显示实际导入的数量（可能为 0 如果都是旧数据）

#### 场景 C: 导入旧版本数据

如果有旧版本（只包含 tracks）的备份：

```json
{
  "tracks": [...],
  "version": "2.0"
}
```

1. **导入旧备份**
2. **验证向后兼容**：
   - [ ] 成功导入作品
   - [ ] 显示警告：缺少工作区/项目信息
   - [ ] 不会破坏现有数据

### 4. 测试错误处理

#### 测试 A: 无效的 JSON 文件

1. 创建一个文本文件：`invalid.json`
   ```
   这不是有效的 JSON
   ```
2. 尝试导入
3. **预期结果**：
   - [ ] 显示错误消息：`❌ 导入失败：...`
   - [ ] 不破坏现有数据

#### 测试 B: 空文件

1. 创建空的 JSON 文件：`empty.json`
   ```json
   {}
   ```
2. 尝试导入
3. **预期结果**：
   - [ ] 显示错误：`无效的数据格式：缺少版本信息`

#### 测试 C: 损坏的数据

1. 编辑导出的 JSON，删除一些必要字段
2. 尝试导入
3. **预期结果**：
   - [ ] 优雅地处理错误
   - [ ] 显示有意义的错误消息

## 🎯 高级功能测试

### 数据冲突解决

**场景**：两个设备上都修改了同一个作品

1. **设备 A**：
   - 导出备份 A
   - 修改作品 1 的名称为 "新名称 A"
   - 等待 1 分钟

2. **设备 B**：
   - 导入备份 A
   - 修改作品 1 的名称为 "新名称 B"
   - 导出备份 B

3. **设备 A**：
   - 导入备份 B

4. **预期结果**：
   - [ ] 作品 1 的名称是 "新名称 B"（因为它的 `updatedAt` 更新）
   - [ ] 使用最新的数据版本

### 大数据量测试

1. **创建大量数据**：
   - 5 个工作区
   - 每个工作区 10 个项目
   - 每个项目 20 个作品
   - 总计：5 × 10 × 20 = 1000 个作品

2. **导出测试**：
   - [ ] 导出成功
   - [ ] 文件大小合理（< 5MB）
   - [ ] 导出速度快（< 2 秒）

3. **导入测试**：
   - [ ] 导入成功
   - [ ] 导入速度快（< 5 秒）
   - [ ] 页面响应正常

## 🔍 数据完整性验证

### 验证脚本

在浏览器控制台运行：

```javascript
// 导出数据
const exportBtn = document.querySelector('[title="导出备份"]')
exportBtn.click()

// 等待下载完成后，检查数据
const data = JSON.parse(localStorage.getItem('pattr_workspaces') || '[]')
console.log('工作区数量:', data.length)

const projects = JSON.parse(localStorage.getItem('pattr_projects') || '[]')
console.log('项目数量:', projects.length)

const tracks = JSON.parse(localStorage.getItem('pattr_tracks') || '[]')
console.log('作品数量:', tracks.length)

// 验证关联关系
projects.forEach(project => {
  const workspace = data.find(w => w.id === project.workspaceId)
  if (!workspace) {
    console.error('孤立的项目:', project.name)
  }
})

tracks.forEach(track => {
  const project = projects.find(p => p.id === track.projectId)
  if (!project) {
    console.error('孤立的作品:', track.name)
  }
})

console.log('✅ 数据完整性检查完成')
```

## 📊 性能基准

| 操作 | 数据量 | 预期时间 |
|------|--------|----------|
| 导出 | 100 作品 | < 500ms |
| 导出 | 1000 作品 | < 2s |
| 导入 | 100 作品 | < 1s |
| 导入 | 1000 作品 | < 5s |
| 文件大小 | 100 作品 | < 500KB |
| 文件大小 | 1000 作品 | < 5MB |

## ✅ 测试清单

### 基本功能
- [ ] 导出功能正常
- [ ] 导入功能正常
- [ ] 文件命名正确
- [ ] 数据格式正确

### 数据完整性
- [ ] 包含所有工作区
- [ ] 包含所有项目
- [ ] 包含所有作品
- [ ] 包含活跃状态
- [ ] 关联关系正确

### 智能合并
- [ ] 基于更新时间合并
- [ ] 不创建重复数据
- [ ] 保留最新版本

### 错误处理
- [ ] 无效文件提示错误
- [ ] 空文件提示错误
- [ ] 损坏数据优雅处理

### 用户体验
- [ ] 成功消息清晰
- [ ] 错误消息有帮助
- [ ] 导入后自动刷新
- [ ] 操作响应快速

## 🐛 已知问题

目前无已知问题。

## 📝 测试报告模板

```
测试日期: YYYY-MM-DD
测试人员: [你的名字]
浏览器: Chrome/Firefox/Safari [版本]

测试结果:
- 导出功能: [ ] 通过 [ ] 失败
- 导入功能: [ ] 通过 [ ] 失败
- 数据完整性: [ ] 通过 [ ] 失败
- 错误处理: [ ] 通过 [ ] 失败

问题记录:
1. [描述问题]
2. [描述问题]

建议:
1. [改进建议]
2. [改进建议]
```

## 🎉 总结

新的导入导出功能提供了：
- ✅ 完整的数据备份
- ✅ 智能数据合并
- ✅ 向后兼容
- ✅ 良好的错误处理
- ✅ 清晰的用户反馈

**建议**：定期导出备份，特别是在重要操作前！

